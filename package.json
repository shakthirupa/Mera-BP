{
  "name": "merabp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && mvnw.cmd spring-boot:run",
    "frontend": "cd frontend && npm start",
    "dev": "start cmd /k \"cd backend && mvnw.cmd spring-boot:run\" && start cmd /k \"cd frontend && npm start\""
  }
}
