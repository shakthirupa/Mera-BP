# Production / cloud — set env vars on your host (Docker, VPS, Railway, etc.)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db:5432/healthcare}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD}

spring.ai.ollama.base-url=${OLLAMA_BASE_URL:http://ollama:11434}

# Disable SQL logging in production
spring.jpa.show-sql=false
